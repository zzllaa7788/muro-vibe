# 🚀 자동 배포 가이드

## 🎯 한 번만 설정하면, 이후는 명령어 하나로 자동 배포! 🐶

---

## 📋 사전 준비

### 1️⃣ 로컬 환경 변수 설정

```bash
# .env.example을 복사하여 .env 파일 생성
cp .env.example .env

# .env 파일을 열고 실제 API 키 입력
notepad .env  # Windows
# 또는
nano .env     # Mac/Linux
```

`.env` 파일 내용:
```env
VITE_GEMINI_API_KEY=여기에_실제_API_키_입력
```

> 💡 API 키 발급: https://aistudio.google.com/app/apikey

---

## 🎉 첫 배포 (한 번만 실행)

### 방법 1: 자동 스크립트 (권장) ⭐

```bash
npm run deploy:first
```

이 명령어가 자동으로:
1. ✅ Netlify 로그인 (브라우저가 열림)
2. ✅ `.env` 파일을 읽어서 환경 변수 자동 설정
3. ✅ 빌드 및 배포

---

### 방법 2: 단계별 실행

#### 1단계: Netlify CLI 로그인
```bash
npx netlify login
```
→ 브라우저가 열리면 로그인하세요!

#### 2단계: Netlify 사이트 연결 (처음 한 번만)
```bash
npx netlify init
```

다음과 같이 선택:
- **What would you like to do?** → `Create & configure a new site`
- **Team:** 본인의 팀 선택
- **Site name:** 원하는 이름 (예: `muro-vibe-dog`)
- **Build command:** `npm run build`
- **Publish directory:** `dist`

#### 3단계: 환경 변수 자동 설정
```bash
npm run setup-env
```
→ 로컬 `.env` 파일의 내용을 Netlify에 자동으로 설정합니다!

#### 4단계: 배포
```bash
npm run deploy
```

---

## 🔄 이후 배포 (매우 간단!)

**첫 배포 이후에는 환경 변수가 이미 설정되어 있으므로:**

```bash
npm run deploy
```

**단 하나의 명령어로 빌드 + 배포 완료!** 🎉

---

## 📝 사용 가능한 명령어

| 명령어 | 설명 |
|--------|------|
| `npm run setup-env` | 로컬 `.env` → Netlify 환경 변수 자동 설정 |
| `npm run deploy` | 빌드 + 프로덕션 배포 |
| `npm run deploy:first` | 환경 변수 설정 + 빌드 + 배포 (첫 배포용) |
| `npx netlify deploy` | 배포 프리뷰 (테스트용) |
| `npx netlify deploy --prod` | 프로덕션 배포 |
| `npx netlify open` | Netlify 대시보드 열기 |

---

## 🔧 자동화 원리

### 1. `scripts/setup-env.js`
- 로컬 `.env` 파일을 읽음
- Netlify CLI를 사용하여 환경 변수 자동 설정
- `netlify env:set` 명령어로 각 변수 등록

### 2. `npm run deploy`
- `vite build`로 프로덕션 빌드
- `netlify deploy --prod`로 자동 배포
- 환경 변수는 이미 설정되어 있으므로 API 정상 작동

---

## ⚠️ 주의사항

### ✅ `.env` 파일은 Git에 커밋하지 마세요!
- `.gitignore`에 이미 포함되어 있음
- API 키가 공개 저장소에 노출되면 보안 위험!

### ✅ 환경 변수 업데이트 시
로컬 `.env` 파일을 수정한 후:
```bash
npm run setup-env  # Netlify 환경 변수 업데이트
npm run deploy     # 재배포
```

### ✅ 새로운 환경 변수 추가 시
1. `.env` 파일에 추가
2. `.env.example` 파일에도 예시 추가 (실제 값은 넣지 말 것!)
3. `npm run setup-env` 실행

---

## 🐕 문제 해결

### ❌ "Netlify CLI not found" 에러
```bash
npm install -D netlify-cli
```

### ❌ ".env 파일이 없다개!" 에러
```bash
cp .env.example .env
# .env 파일을 열고 실제 API 키 입력
```

### ❌ "Not logged in" 에러
```bash
npx netlify login
```

### ❌ "No site configured" 에러
```bash
npx netlify init
```

---

## 🎊 요약

### 🆕 처음 배포할 때:
```bash
npm run deploy:first
```

### 🔄 이후 배포할 때:
```bash
npm run deploy
```

### 🔐 환경 변수만 업데이트:
```bash
npm run setup-env
```

---

## 🌟 장점

- ✅ **수동 설정 불필요**: 대시보드에서 일일이 설정할 필요 없음
- ✅ **보안 유지**: `.env` 파일은 Git에 커밋되지 않음
- ✅ **빠른 배포**: 명령어 하나로 배포 완료
- ✅ **환경 변수 동기화**: 로컬과 Netlify 환경이 항상 일치
- ✅ **팀 협업**: `.env.example`로 팀원들에게 가이드 제공

---

멍멍! 이제 배포가 훨씬 쉬워졌다개! 🐶✨

궁금한 점이 있으면 언제든 물어보개! 🐾

