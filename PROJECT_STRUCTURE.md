# 📁 물어봐이브 프로젝트 구조

## 🎯 Path Alias 설정

```javascript
{
  '@ui': './src/shared/ui',           // 공용 UI 컴포넌트
  '@pages': './src/pages',            // 페이지 컴포넌트
  '@features': './src/features',      // 기능 단위 컴포넌트
  '@widgets': './src/widgets',        // 위젯 컴포넌트
  '@api': './src/shared/api',         // API 연동
  '@lib': './src/shared/lib',         // 유틸리티 함수
  '@config': './src/shared/config',   // 환경 설정
}
```

## 📂 폴더 구조 (FSD Architecture)

```
src/
│
├── 📱 app/                          # 앱 설정 레이어
│   ├── App.jsx                      # 메인 앱 컴포넌트
│   └── App.css                      # 앱 전역 스타일
│
├── 📄 pages/                        # 페이지 레이어
│   └── main-page/
│       ├── MainPage.jsx             # ✅ @pages/main-page
│       ├── MainPage.css
│       └── index.js
│       │
│       └── 📊 State 관리:
│           ├── prompt (질문)
│           ├── result (답변)
│           ├── isLoading (로딩 상태)
│           └── error (에러 상태)
│
├── 🧩 widgets/                      # 위젯 레이어
│   └── result-display/
│       ├── ResultDisplay.jsx        # ✅ @widgets/result-display
│       ├── ResultDisplay.css
│       └── index.js
│       │
│       └── 🎨 UI 상태:
│           ├── Empty State (빈 상태)
│           ├── Loading State (로딩)
│           ├── Error State (에러)
│           └── Success State (성공)
│
├── ⚡ features/                     # 기능 레이어
│   ├── question-input/
│   │   ├── QuestionInput.jsx        # ✅ @features/question-input
│   │   ├── QuestionInput.css
│   │   └── index.js
│   │
│   └── ai-answer/                   # (향후 확장 가능)
│
└── 🔧 shared/                       # 공유 레이어
    │
    ├── ui/                          # ✅ 공용 UI (@ui)
    │   ├── Button/
    │   │   ├── Button.jsx           # 버튼 컴포넌트 (primary, secondary, outline)
    │   │   ├── Button.css
    │   │   └── index.js
    │   │
    │   ├── Loader/
    │   │   ├── Loader.jsx           # 로딩 스피너
    │   │   ├── Loader.css
    │   │   └── index.js
    │   │
    │   ├── TextArea/
    │   │   ├── TextArea.jsx         # 텍스트 입력 영역
    │   │   ├── TextArea.css
    │   │   └── index.js
    │   │
    │   └── index.js                 # 통합 export
    │
    ├── api/                         # ✅ API 연동 (@api)
    │   └── gemini.js                # Gemini API 클라이언트
    │
    ├── lib/                         # ✅ 유틸리티 (@lib)
    │   └── clipboard.js             # 클립보드 복사 함수
    │
    └── config/                      # ✅ 설정 (@config)
        └── env.js                   # 환경 변수 관리
```

## 🎨 UI 레이아웃 구조

### 메인 페이지 (MainPage.jsx)

```
┌─────────────────────────────────────────────────────┐
│  🔮 물어봐이브                                         │
│  일상의 사소한 고민부터 창의적인 아이디어까지...        │
└─────────────────────────────────────────────────────┘
                        ▼
┌─────────────────────────────────────────────────────┐
│  [QuestionInput Component]                          │
│  ┌──────────────────────────────────────────────┐  │
│  │ 무엇이든 물어보세요! 🤔                       │  │
│  │ ┌──────────────────────────────────────────┐│  │
│  │ │ TextArea (5 rows)                       ││  │
│  │ │ "예: 오늘 점심 메뉴로..."               ││  │
│  │ └──────────────────────────────────────────┘│  │
│  │ 💡 힌트 메시지                               │  │
│  └──────────────────────────────────────────────┘  │
│                                                     │
│  [Button - Primary]                                │
│  물어보기! 🚀                                       │
└─────────────────────────────────────────────────────┘
                        ▼
┌─────────────────────────────────────────────────────┐
│  [ResultDisplay Widget]                             │
│                                                     │
│  State 1: Empty                                     │
│  ┌──────────────────────────────────────────────┐  │
│  │           💬                                 │  │
│  │  궁금한 것을 물어보면                         │  │
│  │  이곳에 답변이 표시됩니다!                    │  │
│  └──────────────────────────────────────────────┘  │
│                                                     │
│  State 2: Loading                                   │
│  ┌──────────────────────────────────────────────┐  │
│  │           🔄                                 │  │
│  │    물어봐이브가 고민 중... 🤔💭              │  │
│  └──────────────────────────────────────────────┘  │
│                                                     │
│  State 3: Error                                     │
│  ┌──────────────────────────────────────────────┐  │
│  │           ❌                                 │  │
│  │    앗, 문제가 생겼어요!                       │  │
│  │    [에러 메시지]                             │  │
│  │    [다시 시도하기 🔄]                         │  │
│  └──────────────────────────────────────────────┘  │
│                                                     │
│  State 4: Success                                   │
│  ┌──────────────────────────────────────────────┐  │
│  │  물어봐이브의 답변 ✨                         │  │
│  │  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │  │
│  │                                              │  │
│  │  [AI 생성 답변 텍스트]                       │  │
│  │  여러 줄로 표시되며 스크롤 가능               │  │
│  │                                              │  │
│  │  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │  │
│  │  [복사하기 📋]  [다른 답변 보기 🔄]          │  │
│  └──────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
                        ▼
┌─────────────────────────────────────────────────────┐
│  💡 팁: Ctrl + Enter로 빠르게 질문하세요!           │
│  Powered by Google Gemini AI                       │
└─────────────────────────────────────────────────────┘
```

## 📊 State 관리 (useState)

### MainPage Component

```javascript
const [prompt, setPrompt] = useState('');        // 사용자 질문
const [result, setResult] = useState('');        // AI 답변
const [isLoading, setIsLoading] = useState(false); // 로딩 상태
const [error, setError] = useState(null);        // 에러 메시지
```

### ResultDisplay Widget (Local State)

```javascript
const [copySuccess, setCopySuccess] = useState(false); // 복사 성공 여부
```

## 🎯 컴포넌트 import 사용 예시

### ✅ App.jsx
```javascript
import { MainPage } from '@pages/main-page';
import { validateEnv } from '@config/env';
```

### ✅ MainPage.jsx
```javascript
import { QuestionInput } from '@features/question-input';
import { ResultDisplay } from '@widgets/result-display';
import { Button } from '@ui';
import { generateAnswer } from '@api/gemini';
```

### ✅ QuestionInput.jsx
```javascript
import { TextArea } from '@ui';
```

### ✅ ResultDisplay.jsx
```javascript
import { Button, Loader } from '@ui';
import { copyToClipboard } from '@lib/clipboard';
```

### ✅ gemini.js
```javascript
import { ENV } from '@config/env';
```

## 🎨 UI 컴포넌트 라이브러리 (@ui)

### Button
- **Variants**: `primary`, `secondary`, `outline`
- **Props**: `children`, `onClick`, `disabled`, `variant`
- **스타일**: 그라데이션, 호버 효과, 애니메이션

### Loader
- **Props**: `message`
- **기능**: 회전 애니메이션, 맥박 효과

### TextArea
- **Props**: `value`, `onChange`, `placeholder`, `disabled`, `rows`
- **기능**: 포커스 효과, 반응형

## 🔄 사용자 플로우

1. **질문 입력** → QuestionInput (TextArea)
2. **버튼 클릭** → handleSubmit()
3. **상태 변경** → isLoading = true
4. **API 호출** → generateAnswer(prompt)
5. **결과 표시** → ResultDisplay (Success State)
6. **추가 액션**
   - 복사하기 → copyToClipboard()
   - 다른 답변 보기 → handleRetry()

## 🚀 실행 방법

```bash
# 개발 서버 실행
npm run dev

# 코드 포맷팅
npm run format

# 프로덕션 빌드
npm run build
```

---

**✨ 깔끔한 FSD 구조로 관리되는 물어봐이브 프로젝트!**

